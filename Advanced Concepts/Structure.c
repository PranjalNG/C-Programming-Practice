/*#include <stdio.h>

// Defining a structure
struct Person {
    char name[50];
    int age;
    float height;
};

int main() {
    // Declaring a variable of type struct Person
    struct Person person1;

    // Accessing and assigning values to the structure members
    strcpy(person1.name, "John");
    person1.age = 25;
    person1.height = 6.1;

    // Accessing structure members and printing them
    printf("Name: %s\n", person1.name);
    printf("Age: %d\n", person1.age);
    printf("Height: %.2f\n", person1.height);

    return 0;
}*/



/*#include <stdio.h>

int main() {
    int number = 10;  // Declaring an integer variable
    int *ptr;         // Declaring a pointer variable

    ptr = &number;    // Assigning the address of 'number' to the pointer 'ptr'

    printf("Value of number: %d\n", number);
    printf("Address of number: %p\n", &number);
    printf("Value of ptr: %p\n", ptr);   // Printing the value stored in 'ptr' (memory address)
    printf("Value at the address pointed by ptr: %d\n", *ptr);  // Dereferencing 'ptr' to get the value at that address

    return 0;
}*/



